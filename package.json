{
  "name": "custom-proxy-rules",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Build proxy rules and generate private client configurations in the browser.",
  "scripts": {
    "build": "node scripts/build-rules.mjs",
    "build:local": "node scripts/build-rules.mjs --no-fetch",
    "build:lists": "node scripts/build-rules.mjs --lists-only",
    "build:web": "node scripts/build-web.mjs",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "dependencies": {
    "qrcode": "^1.5.4",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
